flows:
- actions:
  - send_menu:
      buttons:
      - label: Details
        payload:
          id: '{{$nerd_id}}'
        type: postback
      - label: Something
        payload: Any random text
        type: postback
      - label: ping
        payload: pong
        type: message
      - label: More Details
        payload: http://www.wizeline.com/profile
        type: open_url
  name: main
version: 2.1.0
